import React from 'react';
import {
  Text,
  View,
  Image,
  ScrollView,
  AsyncStorage,
  Dimensions,
} from 'react-native';

export default class PlaylistScreen extends React.Component {
  state = {
    title: '',
    tracks: [],
  };
  componentDidMount() {}
  render() {
    return <View />;
  }
}
